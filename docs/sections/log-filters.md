# Log Filters

You can control which events will be logged to the game log. Disable any option if the log message in that category get too excessive. Some log entries are generated by KS, others are generated by the game itself.

This feature also replaces the log filter settings the game usually provides itself.
